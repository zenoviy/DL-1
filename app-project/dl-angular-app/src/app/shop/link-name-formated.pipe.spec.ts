import { LinkNameFormatedPipe } from './link-name-formated.pipe';

describe('LinkNameFormatedPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkNameFormatedPipe();
    expect(pipe).toBeTruthy();
  });
});
